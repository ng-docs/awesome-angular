import { PartialScrollerDirective } from './partial-scroller.directive';

describe('PartialScrollerDirective', () => {
  it('should create an instance', () => {
    const directive = new PartialScrollerDirective();
    expect(directive).toBeTruthy();
  });
});
