import { ShowWhenActiveDirective } from './show-when-active.directive';

describe('ShowWhenActiveDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowWhenActiveDirective();
    expect(directive).toBeTruthy();
  });
});
