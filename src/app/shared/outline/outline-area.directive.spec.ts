import { OutlineAreaDirective } from './outline-area.directive';

describe('OutlineAreaDirective', () => {
  it('should create an instance', () => {
    const directive = new OutlineAreaDirective();
    expect(directive).toBeTruthy();
  });
});
