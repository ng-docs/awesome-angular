import { OutlineHostDirective } from './outline-host.directive';

describe('OutlineHostDirective', () => {
  it('should create an instance', () => {
    const directive = new OutlineHostDirective();
    expect(directive).toBeTruthy();
  });
});
