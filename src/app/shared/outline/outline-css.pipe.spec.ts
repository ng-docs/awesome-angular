import { OutlineCssPipe } from './outline-css.pipe';

describe('OutlineCssPipe', () => {
  it('create an instance', () => {
    const pipe = new OutlineCssPipe();
    expect(pipe).toBeTruthy();
  });
});
